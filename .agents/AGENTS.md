# Project Custom Rules

- Do not run tests, push, or commit code directly ("ko run test, không push, khong commit").
- When the user asks for a commit ("đưa tui commit" or similar), output ONLY the exact commit message string itself. Do not include git command boilerplate, headers, explanations, or extra bullet points.
