import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Light colors
  static const primaryLight = Color(0xFF4C4DDC);
  static const primaryContainerLight = Color(0xFFE4E4FA);
  static const backgroundLight = Color(0xFFFFFFFF);
  static const surfaceLight = Color(0xFFF8F8F8);
  static const cardLight = Color(0xFFFFFFFF);
  static const borderLight = Color(0xFFE1E1E1);
  static const textPrimaryLight = Color(0xFF101010);
  static const textSecondaryLight = Color(0xFF666666);
  static const successLight = Color(0xFF22C55E);
  static const warningLight = Color(0xFFF59E0B);
  static const errorLight = Color(0xFFEF4444);

  // Luminous Royal Midnight Navy Dark colors (Navy Mirage Theme)
  static const primaryDark = Color(0xFF6366F1); // Luminous Blue-Indigo Accent
  static const primaryContainerDark = Color(0xFF3F5E96); // Steel Blue Container (#3F5E96)
  static const backgroundDark = Color(0xFF141E30); // Rich Navy Mirage (#141E30)
  static const surfaceDark = Color(0xFF1A263B); // Dark Navy Surface
  static const cardDark = Color(0xFF1E2D47); // Luminous Elevated Navy Mirage Card
  static const borderDark = Color(0xFF2B3D5C); // Navy Mirage Border
  static const textPrimaryDark = Color(0xFFFFFFFF); // Pure Crisp White Text
  static const textSecondaryDark = Color(0xFFCBD5E1); // Bright Slate-Silver Text (High Contrast)
  static const successDark = Color(0xFF4ADE80); // Emerald Green Accent
  static const warningDark = Color(0xFFFBBF24); // Warm Amber
  static const errorDark = Color(0xFFF87171); // Soft Coral Red
}
